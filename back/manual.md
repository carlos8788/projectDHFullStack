- primero ejecutamos `npm run dev` y luego `npm run gulp`
 Esto nos permite ver en el momento si hacemos cambios en el css y ejs

- Para terminar un proceso de TCP 
  - `netstat -ano | findstr :PORT` 
  - `taskkill /PID PORT /F`